'use strict';

/**
 * current-tournament router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::current-tournament.current-tournament');
